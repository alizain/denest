import React from "react"

import "./style.css"

export default function LoadingCard({ error }) {
  return (
    <section className="loading text-center sans-serif">
      <div className="icon" />
    </section>
  )
}
